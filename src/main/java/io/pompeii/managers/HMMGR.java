package io.pompeii.managers;

import java.util.HashMap;
import java.util.UUID;

public class HMMGR {
	
	public static HashMap<UUID, Long> MASS_BAN_PREVENTER = new HashMap<UUID, Long>();
	public static HashMap<UUID, Long> PANIC = new HashMap<UUID, Long>();
	public static HashMap<UUID, Long> REQUEST_COOLDOWN = new HashMap<UUID, Long>();
	
	
}
